func (i Item) PrintWithPrice() string {
	// present the item in the format Name: PriceCurrencyCode
	// NOTE: fmt.Sprintf allows us to format strings just like with fmt.Println
	// Use format string "%s: %v%s"
}
