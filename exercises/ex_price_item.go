func (i Item) PrintWithPrice() string {
	// present the item in the format Name: PriceCurrency
	// NOTE: fmt.Sprintf allows us to format strings and
	// return them just like we printed with fmt.Println
}// method should be exported
