package repo
type DB interface {
    //Define methods signatures here for all methods called from the OrderRepo
    //Update reference to the DB from OrderRepo to the abstract type
    //Signatures must match those of the in memory database
}
